<Window x:Class="PackageManager.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:C="clr-namespace:CustomControlLibrary.CustomControl.Controls.DataGrid;assembly=CustomControlLibrary"
        xmlns:button="clr-namespace:CustomControlLibrary.CustomControl.Controls.Button;assembly=CustomControlLibrary"
        xmlns:controls="clr-namespace:CustomControlLibrary.CustomControl.Controls;assembly=CustomControlLibrary"
        Title="日志查看器" Height="600" Width="1500"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 控件区 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="日志类型:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <controls:CComboBox x:Name="LogTypeCombo" Width="120" SelectedIndex="1" Margin="0,0,12,0">
                <ComboBoxItem Content="常规日志"/>
                <ComboBoxItem Content="错误日志"/>
            </controls:CComboBox>

            <TextBlock Text="日期:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <controls:CComboBox x:Name="DateCombo" Width="130" Margin="0,0,12,0"/>

            <TextBlock Text="级别:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <controls:CComboBox x:Name="LevelCombo" Width="110" SelectedIndex="0" Margin="0,0,12,0">
                <ComboBoxItem Content="全部"/>
                <ComboBoxItem Content="INFO"/>
                <ComboBoxItem Content="WARN"/>
                <ComboBoxItem Content="ERROR"/>
            </controls:CComboBox>

            <controls:CTextBox x:Name="SearchTextBox" Width="220" Margin="0,0,12,0"/>
            <button:CButton Content="刷新" Width="100" Click="RefreshButton_Click"/>
        </StackPanel>

        <!-- 日志表格（使用自定义控件） -->
        <C:CDataGrid x:Name="LogGrid" Grid.Row="1"
                     TextAlignment="Left"
                     EnableAutoTemplateSelection="True"
                     Margin="0,0,0,0"/>

        <!-- 底部操作 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <button:CButton Content="打开日志目录" Width="140" Margin="0,0,10,0" Click="OpenDirectoryButton_Click"/>
            <button:CButton Content="关闭" Width="100" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>