<Page x:Class="PackageManager.Views.PackageConfigPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:C="clr-namespace:CustomControlLibrary.CustomControl.Controls.DataGrid;assembly=CustomControlLibrary"
      xmlns:button="clr-namespace:CustomControlLibrary.CustomControl.Controls.Button;assembly=CustomControlLibrary"
      mc:Ignorable="d"
      Title="包管理配置">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="包配置（内置不可修改，自定义可编辑）" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="12,0,0,0">
                <button:CButton Content="新增" Width="80" Click="AddButton_Click"/>
                <button:CButton Content="保存" Width="80" Margin="8,0,0,0" Click="SaveButton_Click"/>
                <button:CButton Content="可见性控制" Width="100" Margin="8,0,0,0" Click="OpenVisibilityPanel_Click"/>
                <button:CButton Content="返回" Width="80" Margin="8,0,0,0" Click="BackButton_Click"/>
            </StackPanel>
        </StackPanel>

        <C:CDataGrid x:Name="PackageGrid" Grid.Row="1"
                     TextAlignment="Left"
                     EnableAutoTemplateSelection="True"
                     ItemsSource="{Binding AllItems}"
                     SelectedItem="{Binding SelectedItem}"/>

        <Grid x:Name="VisibilityPanel" Grid.RowSpan="2" Background="#80000000" Visibility="Collapsed">
            <Border Background="White" Padding="16" Margin="80" CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="产品包可见性设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                    <C:CDataGrid x:Name="VisibilityGrid" Grid.Row="1"
                                 EnableAutoTemplateSelection="True"
                                 ItemsSource="{Binding VisibilityItems}"/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <button:CButton Content="确定" Width="80" Click="ConfirmVisibilityButton_Click"/>
                        <button:CButton Content="取消" Width="80" Margin="8,0,0,0" Click="CancelVisibilityButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
